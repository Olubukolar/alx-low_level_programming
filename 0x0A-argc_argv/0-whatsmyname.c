#include "main.h"

/**
 * main - the function of the prototype
 * :argc: argument counts
 * @argv: list the content of command line in argument
 * Return: Always (0)
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
