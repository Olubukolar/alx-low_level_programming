#include "main.h"

/**
 * print_number - function that prints an integer.
 * @n: parameter to be checked
 * Return: Always (0)
 */
void print_number(int n)
{
	int i;

	for (i = 0 ; i <= n ; i++)
	{
		_putchar ('n');
		i++;
	}
}
